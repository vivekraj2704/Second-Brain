import { ReactElement } from "react"

interface CardInterface{
    svg: ReactElement,
    text: string,
}

export function Card(props: CardInterface) {
    return(
        <>
            
        </>
    )
}